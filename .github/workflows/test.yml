name: Build/lint/test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-Dwarnings"

jobs:
  check-format:
    name: "Check code formatting"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/workflows/rustup.yml
      - name: Check formatting
        run: cargo fmt --check

  build:
    name: "Build"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/workflows/rustup.yml
      - uses: ./.github/workflows/gtk.yml
      - name: Build
        run: cargo build --verbose

  lint:
    name: "Run clippy"
    runs-on: ubuntu-latest
    needs: [ "build" ]
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/workflows/rustup.yml
      - uses: ./.github/workflows/gtk.yml
      - name: Run clippy
        run: cargo clippy --all-targets --all-features

  test:
    name: "Run tests"
    runs-on: "ubuntu-latest"
    needs: [ "build" ]
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/workflows/rustup.yml
      - uses: ./.github/workflows/gtk.yml
      - name: Run tests
        run: cargo test --verbose
