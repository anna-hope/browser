name: Rustup
description: Install rustup

runs:
  using: composite
  steps:
    - name: Install Rust
      shell: sh
      run: curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
